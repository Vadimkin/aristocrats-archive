{
  "name": "aristocrats-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "dev": "npm run data && vite",
    "build": "npm run data && vite build",
    "preview": "vite preview",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:import": "node scripts/import-tracks.mjs",
    "db:parse-aristocrats-podcasts": "node scripts/parse-aristocrats-podcasts.mjs"
  },
  "dependencies": {
    "@preact/signals": "^2.0.1",
    "preact": "^10.26.4",
    "wouter-preact": "^3.6.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "better-sqlite3": "^13.0.3",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "vite": "^6.2.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
